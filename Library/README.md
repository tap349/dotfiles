Don't use symlinks for these folders and files within:

- Keyboard Layouts
- Preferences

For Emacs keybindings to work in iTerm2 set:

Settings → Profiles → Default → Keys (tab):

```
Left option key: Esc+
```
